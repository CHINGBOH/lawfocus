# 学习工具产品需求与验收标准

> 定义"法律体系概念穿透学习工具"的用户任务、界面形态、职责边界和验收标准。数据模型见 `01-法律体系概念图谱设计.md`。

## 1. 产品目标

面向想要系统学习某部法律（及其在整个法律体系中位置）的个人或小团队，提供一个以条文阅读为入口、以概念穿透溯源为核心的学习工具。

核心闭环：

```text
选择/检索一部法律 → 按目录浏览条文 → 点击概念查看定义与来源
→ 沿关系穿透到上下位概念、跨法引用条文 → 回到原阅读上下文继续学习
```

系统只组织和呈现已核验的法律内容，不产出合规判断，不替代法律咨询。

## 2. 用户与核心任务

| 用户 | 核心任务 | 期望结果 |
|---|---|---|
| 学习者（法律相关专业学生、从业者、感兴趣的个人） | 系统学习一部法律，理解概念体系 | 打开任意条文，点开任意概念都能看到定义、来源、上下文关系 |
| 内容维护者（可以是同一个人） | 导入法律原文、核对概念定义与关系 | 标记内容为 `VERIFIED`，供学习者信赖展示 |

不设"合规人员""审核人员""审计员"等企业角色——旧规约里的四类用户被收敛为这两类。

## 3. 范围

### 3.1 P0 必须交付

1. **法律仓库**：至少收录一部完整法律（现行版本），条文可按目录浏览，来源和版本可查。
2. **条文阅读器**：目录 + 正文 + 相邻条文导航；正文中的概念以超链接形式呈现。
3. **概念溯源浮层**：点击概念展示定义、来源条文、审核状态；提供"深入查看"进入概念详情。
4. **跨法穿透面板**：概念详情页展示该概念在其他法律中的引用/关联，附来源出处；无已核验关系时明确显示"暂无已核验的跨法关联"。
5. **检索**：按法律名、条号、概念名进入内容,返回命中位置和版本信息。

### 3.2 后续可做（P1，不阻塞首个可用版本）

- 多部法律同时收录、法律间体系图可视化。
- 版本对比（新旧条文 diff）。
- 用户笔记/收藏/学习进度。

### 3.3 明确不做

以下能力全部来自旧的"上市公司治理合规系统"定位，本次收敛后**不做**，不在 `apps/` 现有代码基础上继续扩展：

- 不做确定性规则引擎、五值逻辑执行、规则草稿/审核/发布状态机。
- 不做治理事实与证据录入、合规检查工作流、结论证明链。
- 不做企业级 RBAC（8 角色）、多租户隔离、审计追踪。
- 不做真实公司材料的采集、分级、脱敏、留存与删除。
- 不自动给出"合规/违规"判断，不做风险评分。

## 4. 功能需求

| 编号 | 功能 | 必要行为 |
|---|---|---|
| FR-01 | 法律版本 | 原文只增不改；条文可定位到具体版本和有效期间 |
| FR-02 | 阅读与检索 | 按法律、章节、条号打开条文；搜索结果显示命中版本 |
| FR-03 | 概念溯源 | 概念链接展示定义、来源、审核状态；`text_segments[]` 驱动渲染,不做客户端字符串匹配 |
| FR-04 | 跨法穿透 | 概念详情展示跨法关系,每条关系附来源;无已核验关系时显示空态而非模型生成内容 |
| FR-05 | 内容可信度 | 未核验内容在 UI 上与已核验内容视觉区分,不得混同展示 |

## 5. 职责边界

| 层 | 负责 | 不负责 |
|---|---|---|
| 前端 | 阅读、检索、概念浮层、跨法穿透展示、未核验内容的空态提示 | 判定内容是否已核验、拼接不存在的概念关系 |
| 后端 | 法律仓库/概念图谱的存储、版本约束、检索接口 | 用生成文本替代权威数据 |
| RAG 索引 | 基于已入库内容做语义检索,辅助找到相关条文/概念 | 冒充"已核验关系"写入图谱 |
| LLM | 组织解释语言、回答用户追问、生成候选概念关系供人工核对 | 直接创建已核验内容、编造条文或概念关系 |
| 内容维护者 | 核对法源原文与概念定义、标记 `VERIFIED` | 无 |

## 6. 验收场景

### AC-01 条文与概念溯源

给定一个已收录条文,用户打开阅读页并点击"独立董事"（或该法律中的任意已核验概念）。页面显示定义、来源条文、版本和审核状态;返回上级条文时阅读上下文（法律、版本、条号）保持不丢失。

### AC-02 跨法穿透

给定一个在多部法律中都出现的概念,进入其详情页可看到跨法关联列表,每条都能点击跳转到对应法律的对应条文。

### AC-03 未核验内容的空态

给定一个尚未人工核对的概念,页面明确显示"暂无已核验的定义/关联",不生成看似确定的内容,也不静默隐藏该概念不做提示。

### AC-04 检索

给定法律名、条号或概念名的部分匹配输入,搜索结果列出命中项及其法律、版本、位置,点击后正确定位。

## 7. 非功能要求

| 类别 | 指标 |
|---|---|
| 溯源 | 已核验概念关系 100% 关联具体条文或释义来源 |
| 可用性 | 加载中/空数据/未核验/错误四种状态均有独立 UI 呈现,不用同一个空白页笼统表示 |
| 响应 | 条文阅读、概念浮层加载无明显卡顿（个人工具场景,不设企业级 SLO 数值指标） |

## 8. 里程碑

| 阶段 | 产物 | 退出条件 |
|---|---|---|
| M0 数据基础 | 首部法律原文导入、条文切分、`text_segments[]` 标注 | 能按目录浏览完整一部法律 |
| M1 概念图谱首批 | 该法律核心概念的定义、关系人工核对 | AC-01 通过 |
| M2 检索与穿透 | RAG 索引、跨法穿透面板 | AC-02、AC-04 通过 |
| M3 可用版本 | 前端阅读器 + 概念超链接 + 跨法穿透完整可用 | 全部验收场景通过,可用于实际学习 |
