# 法律体系概念图谱设计

> 定义"法律仓库"与"概念图谱"两层结构化真相的数据模型，以及基于其上构建的 RAG 索引。这两层是溯源可信度的底线：LLM 只能在这两层已有内容之上做检索、组织和解释，不得新增概念、关系或条文内容。

## 1. 法源采集原则

1. 原文仅从制定机关、官方法律法规数据库或权威出版物获取；不接受二手转载、新闻解读或征求意见稿作为正式内容来源。
2. 每次采集生成独立 `LegalVersion`，保存原文件、正文哈希（SHA-256）、公布日、施行日、失效日（如有）和来源 URL。
3. 原文只增不改：新版本不覆盖旧版本；已导入内容需要修正时，新建版本记录并标注修正原因，不做原位编辑。
4. 每部法律各自独立管理版本历史；不假设不同法律之间的版本节奏一致。

## 2. 法律仓库数据模型

```text
LegalDocument        一部法律（如《中华人民共和国公司法》）
  ├─ code, name, category（如"商法/民法/..."）
  └─ LegalVersion[]   该法律的历次版本
        ├─ version_name, promulgated_at, effective_at, expired_at（可空）
        ├─ source_url, content_hash
        ├─ review_status: UNVERIFIED | VERIFIED   -- 是否经过人工核对原文与切分
        └─ Article[]    该版本下的条文
              ├─ article_no, chapter_no, section_no, order_in_version
              ├─ text                 -- 条文正文
              └─ text_segments[]      -- 正文分段标注，供概念超链接消费
```

- `Article` 只属于某一个 `LegalVersion`；同一条号在不同版本各自独立成条，不做跨版本合并展示，由前端负责"上一版/下一版对照"的呈现。
- `text_segments[]` 是条文正文的分段结构（起止偏移 + 可选 `concept_id`），前端概念超链接**只能**基于这个结构渲染，不允许用字符串匹配在客户端"猜"概念——这条约束沿用自旧规约，是防止误标概念的硬性要求。

## 3. 概念图谱数据模型

```text
Concept               一个法律概念（如"独立董事"）
  ├─ canonical_name, aliases[]
  └─ ConceptVersion[]  概念定义可能随法律修订变化，故也版本化
        ├─ definition_text
        ├─ defined_in: Article 引用（哪部法律哪一版哪一条定义/首次出现）
        ├─ review_status: UNVERIFIED | VERIFIED
        └─ ConceptRelation[]  与其他概念/条文的关系
              ├─ type: DEFINED_BY | BROADER_THAN | NARROWER_THAN
                      | REFERENCED_IN（跨法引用）| USED_IN_ARTICLE
              ├─ target: ConceptVersion 或 Article 引用
              └─ source_note        -- 该关系判断依据的条文或释义来源
```

- 概念不是条文，也不是规则：一个 `Concept` 可能在多部法律中出现、被不同版本反复定义或引用，`ConceptRelation` 就是承载"跨法穿透"的核心结构。
- 每条 `ConceptRelation` 必须能指回具体的 `Article` 或权威释义来源；不允许只有关系类型没有来源的"孤立关系"——这是防止 LLM 编造概念网络的关键约束。
- `review_status` 只有两档：`UNVERIFIED`（机器抽取，未经人工核对）/ `VERIFIED`（人工确认定义与来源准确）。不需要旧规约里 8 角色 RBAC 与多级审核状态机；一个人也能完成核对工作，机制上只需要这一个布尔位区分"能不能作为可信溯源展示"。

## 4. 内容可信度边界

- 未 `VERIFIED` 的概念定义或关系，前端展示时必须明确标注"未核验"，不得与已核验内容混同呈现。
- 检索/生成链路（RAG + LLM）只能命中数据库中已有的 `Article`、`Concept`、`ConceptRelation` 记录来组织答案；模型输出中如果引入了数据库里不存在的概念关系或条文引用，视为缺陷，需要能被人工核查发现（例如：解释文本必须附带引用的 `article_id`/`concept_relation_id`，无法附带引用的语句不展示为定性结论）。
- 这条边界取代了旧规约里"五值真值系统 + 规则引擎"的角色：旧系统靠确定性规则计算结论，新系统靠"结构化关系是否存在"来判断能不能回答，回答不了就明确说"未找到已核验的溯源关系"，不用模型常识补写。

## 5. RAG 索引设计

- 索引对象：`Article.text`（按条文或语义段落分块）+ `ConceptVersion.definition_text` + 已核验的 `ConceptRelation.source_note`。
- 每个索引块必须携带可回溯的结构化元数据：`law_code`、`version_name`、`article_no`、`concept_id`（如适用），检索结果展示时始终连回法律仓库/概念图谱的原始记录，而不是只返回一段脱离上下文的文本。
- 检索优先级：概念图谱中已有的显式关系 > 全文语义检索命中。即：用户点开一个概念时，先展示图谱里已核验的关系,只在图谱覆盖不到时才补充语义检索结果，并明确标注"语义相关，非已核验关系"。
- 不引入向量相似度作为溯源关系的替代——语义检索只用于"帮用户找到可能相关的条文/概念去看",不能直接冒充"已确认的概念关系"写入图谱。

## 6. 最小 schema 草案

| 表 | 关键字段 | 约束 |
|---|---|---|
| `legal_document` | code, name, category | code 唯一 |
| `legal_version` | document_id, version_name, promulgated_at, effective_at, expired_at, source_url, content_hash, review_status | (document_id, version_name) 唯一；原文追加不改 |
| `article` | version_id, article_no, chapter_no, section_no, text, text_segments(jsonb) | (version_id, article_no) 唯一 |
| `concept` | canonical_name, aliases(jsonb) | canonical_name 唯一 |
| `concept_version` | concept_id, definition_text, defined_in_article_id, review_status | |
| `concept_relation` | from_concept_version_id, type, to_concept_version_id 或 to_article_id, source_note, review_status | 关系必须指向具体版本化对象，不指向"概念"抽象体 |

所有列表接口统一返回 `items/page/page_size/total`；错误响应至少含 `code`、`message`，不返回堆栈或内部路径。这两条沿用自旧规约，是任何后端服务都该有的基本契约,与产品定位无关。
