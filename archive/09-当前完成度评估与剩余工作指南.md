# 当前完成度评估与剩余工作指南

> 评估基线：2026-07-18。完成度以可重复验证证据为准，不以文件数量或旧工作记录为准。

## 1. 结论

项目已越过“骨架搭建”，处于“功能闭环完成、正式数据与发布验收待完成”阶段。

| 维度 | 完成度 | 判断 |
|---|---:|---|
| MVP 工程骨架 | 95% | 前后端、迁移、权限、审计、规则引擎均可运行 |
| 产品功能闭环 | 80% | F0–F5 完成；F6 浏览器自动化未完成 |
| 真实数据链路 | 65% | 公司法 266 条和贵州茅台年报已入库并运行，但仍为 `UNVERIFIED` |
| 正式法律规则 | 33% | `GOV-AUD-002` 已真实绑定公司法第一百二十一条并发布（`MVP-P0-REAL-SOURCED/v1`）；其余 9 条仍是 `MVP-P0-DEMO/v1` 的 DEMO 法源 |
| 部署与生产准入 | 40% | Docker、真实 CI、容量、恢复和故障演练缺少证据 |
| **综合完成度** | **约 76%** | 可演示 MVP，尚不是可对外提供正式合规结论的版本 |

当前验证结果（2026-07-18 本轮更新）：后端 **169** 个 pytest 全绿（新增 1 个真实条文文本回归测试）；`ruff`、`mypy` 全绿；前端测试/lint/build 未在本轮改动，沿用此前 55 个通过的记录。

## 2. 已完成能力

- 法律、条文、概念、事实、证据、规则、结论和证明链的数据模型已落地。
- JWT、RBAC、租户隔离、双审、发布门禁、幂等和审计由后端执行。
- 十条规则具备处理器，五值结果和半开时间区间不是占位实现。
- 法律阅读、公司治理、事实证据、检查结果、证明穿透、规则审核和工作台页面可用。
- 真实《公司法》与贵州茅台年报具备来源 URL、文件哈希和幂等导入脚本。
- 贵州茅台样本已生成持久化检查和证明链，三条演示规则结果为 `TRUE`。
- `GOV-AUD-002` 已完成首条真实法源迁移：真实绑定公司法第一百二十一条、走完真实双审、发布为独立 `RuleSet`（`MVP-P0-REAL-SOURCED/v1`），对贵州茅台真实数据重跑结果为 `TRUE`（`scripts/promote_gov_aud_002.py` + `scripts/run_real_sourced_sample.py`，见第 3 节 P0 执行记录）。

## 3. 关键缺口

### P0：形成正式法律数据闭环

- [ ] 法律审核者核验公司法原件、版本、266 条切分和文本准确性。
- [ ] 导入证监会独立董事规则、上交所上市公司自律监管指引等直接法源。
- [ ] 将十条规则的阈值迁入 `requirement_expression`，处理器不得继续硬编码。
- [ ] 每条规则绑定已审核 `ArticleVersion`，补齐来源、例外、适用范围和有效期。
- [ ] 完成法律审核、技术审核和正式 `RuleSet` 发布；重新运行 600519 样本。
- [ ] 对未披露字段保持 `UNKNOWN`；不得推断审计委员会召集人等事实。

退出条件：正式规则集不引用 `DEMO` 条文，任一结论可穿透至真实条文版本和公司证据。

**执行记录（2026-07-18，本轮）**：完成 P0 的一个真实、狭窄验证切片，而非全部十条规则。

判断：`GOV-AUD-001`（强制设立审计委员会）与 `GOV-AUD-003`（独立董事占过半数）在真实《公司法》文本中找不到直接支持——第一百二十一条原文是"股份有限公司**可以**按照公司章程的规定…设置…审计委员会"（设立本身是可选项，监事会的替代方案），独立性要求原文是"过半数成员**不得在公司担任除董事以外的其他职务**"，并非"过半数须为独立董事"（两者是不同的法律概念）。将这两条规则绑定到公司法条文将构成虚构引用。已就此征得用户确认：本轮只处理 `GOV-AUD-002`（审计委员会成员构成，不少于三人），该条与第一百二十一条"审计委员会成员为三名以上"直接、无歧义对应；`GOV-AUD-001`/`GOV-AUD-003` 明确留待导入证监会/交易所直接法源后处理，不在本轮内声明为已解决。

已完成：新增 `scripts/promote_gov_aud_002.py`（幂等）——将 `GOV-AUD-002` 的法源从 DEMO 文本改绑为真实 `PRC-COMPANY-LAW` 第一百二十一条（`RuleSource` 增删，非编辑已发布内容）；`requirement_expression` 确认为 `{"operator":"gte","value":3,"unit":"person"}`；新增 7 类测试用例（`PASS`/`BOUNDARY` 用贵州茅台真实审计委员会数据真实执行——3 名成员恰好等于阈值，同时验证通过与边界；`VIOLATION` 用新增的丁公司 2 人审计委员会演示夹具真实执行；`NOT_APPLICABLE` 用乙公司真实的"无审计委员会"状态真实执行；`MISSING_FACT`/`EXCEPTION`/`CONFLICT` 因规则基于治理登记表而非事实层、结构上无这三类分支，如实标注为"仅满足强制类型存在性要求，未做真实执行"而非伪造真实执行）；真实调用 `RuleGovernanceService` 走完 submit→法律审核→技术审核→发布，审核意见如实写明"工程流程验证，非人类执业律师签署的正式法律意见"（该文案完整显示在规则详情页审核记录中，任何人查看都能看到这不是真实法律签署）；发布门禁真实重新执行 7 个用例后通过；新增 `RuleSet` `MVP-P0-REAL-SOURCED/v1`（唯一成员即此规则），真实走完 `RuleSetService` 的草稿→发布。新增 `scripts/run_real_sourced_sample.py`，对贵州茅台真实数据重新运行该规则集：结果 `GOV-AUD-002 → TRUE`（真实 3 名审计委员会成员满足≥3），`check_id` 二次执行不变（幂等验证）。新增回归测试 `test_article_121_audit_committee_threshold_text_is_intact`（锁定"三名以上"与"不得担任除董事以外其他职务"两句原文，防止未来重新抓取时静默改变引用依据）。全量验证：后端 **169 pytest 全绿**、`ruff`/`mypy` 全绿；真实浏览器核对规则详情页（reader 账号）——法源显示真实第一百二十一条全文、7 条测试用例、审核记录完整显示工程免责声明文案、状态 PUBLISHED。

遗留：`GOV-AUD-001`/`GOV-AUD-003` 仍是 DEMO 法源，未处理；十条规则中仅 1 条完成真实法源化；证监会/交易所直接法源尚未导入。在导入这些法源前，不得扩大声称"已完成 P0"。

### P1：完成 F6 浏览器 E2E

- [ ] 引入 Playwright，并启动真实 Web、API、PostgreSQL 环境。
- [ ] 浏览器覆盖登录、法律阅读、概念来源、事实证据录入和关联。
- [ ] 覆盖五值结果、证明穿透、规则双审发布及发布失败提示。
- [ ] 覆盖自审禁止、跨租户拒绝、无权限发布和 Agent 禁用降级。
- [ ] 将测试接入 `make e2e`；禁止用 `TestClient` 冒充浏览器验收。

退出条件：`06-MVP骨架充实与功能闭环计划.md` 第 7 节场景在干净数据库中稳定通过。

### P2：完成 F7 发布与韧性验收

- [ ] 在有 Docker 的机器执行 `make bootstrap → up → migrate → seed → test → e2e`。
- [ ] 推送分支并保存 GitHub Actions 成功运行链接。
- [ ] 编写固定随机种子的容量数据生成器和负载脚本。
- [ ] 按性能方案执行 10 分钟负载、2 小时稳定性及容量拐点测试。
- [ ] 演练数据库超时、Agent 不可用、重复请求、备份恢复和回滚。
- [ ] 输出包含 P50/P95/P99、错误率、资源使用和慢查询的验收报告。

退出条件：Docker、CI、性能和故障恢复均有外部运行记录，而非只有配置文件。

### P3：补齐数据治理与运维

- [ ] 明确“公开真实材料能否进入本地/CI”的政策；当前 `05` 号规范存在“PUBLIC 可进入公共知识区”与“本地/CI 仅合成数据”的口径冲突。
- [ ] 将原始 PDF、提取文本和数据库派生记录纳入数据目录及保留策略。
- [ ] 增加证据预览、下载、导出和删除审计；验证可删除与备份恢复周期。
- [ ] 修订 README 中过时的测试数量、“无真实法源”和“仅两家演示公司”等描述。
- [ ] 为真实导入脚本增加 API/UI 入口前，先补权限、审批和失败恢复设计。

## 4. 推荐执行顺序

```text
R1 法源审核与规则参数化
→ R2 发布真实规则集并复跑600519
→ R3 Playwright浏览器E2E
→ R4 Docker与CI验证
→ R5 容量、故障、备份恢复
→ R6 文档校准与发布评审
```

每个阶段都应提交：代码、迁移或契约变更、自动化测试、运行命令、结果证据和已知限制。发现失败时先保存最小复现案例，再修复并加入回归集。

## 5. 下一位 Agent 的首个任务

R1 纵向切片已验证可行，但范围比原计划窄：`GOV-AUD-002` 已真实法源化、双审、发布并对贵州茅台真实重跑（见第 3 节 P0 执行记录）。`GOV-AUD-001`（强制设立审计委员会）与 `GOV-AUD-003`（独立董事占多数）**未**处理——真实公司法第一百二十一条把设立本身列为可选项，独立性标准也只是"不得担任除董事以外其他职务"而非"须为独立董事"，这两条规则需要证监会《上市公司独立董事管理办法》或交易所自律监管指引作为直接法源，绑定到公司法会构成虚构引用。

下一位 Agent 的选择（任选其一，视优先级而定）：

1. **继续 P0**：定位并导入证监会/交易所对应规则的官方文本（需真实来源 URL + 哈希校验，禁止编造条文），复用 `scripts/promote_gov_aud_002.py` 的模式为 `GOV-AUD-001`/`GOV-AUD-003` 建立真实绑定，再复制到其余 7 条规则。
2. **转向 P1（Playwright 浏览器 E2E）**：`09` 与 `07` 两份指南均把这一项列为下一优先级；`06-MVP骨架充实与功能闭环计划.md` 第 7 节场景尚未有真实浏览器自动化覆盖（此前仅有人工 Playwright MCP 走查，非可重复运行的测试套件）。

完成全部 P0 前，不得把 `GOV-AUD-002` 之外的规则结果表述为正式合规结论；即使 `GOV-AUD-002` 本身，PUBLISHED 也只代表工程治理门禁通过，不是人类执业律师签署的正式法律意见（见 `scripts/promote_gov_aud_002.py` 模块文档字符串）。
